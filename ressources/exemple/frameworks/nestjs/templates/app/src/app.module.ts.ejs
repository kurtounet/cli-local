import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { TypeOrmModule } from '@nestjs/typeorm';
//import { databaseConfig } from './database.config';
//import { RateLimitModule } from './rate-limit/rate-limit.module';
<% imports.forEach(module => {
    let moduleName = module.replace(/,\n?/g, '').trim();
    let folder = ejsHelpers.pascalToKebab(moduleName.replace('Module', ''));
_%>
import { <%= moduleName %> } from './modules/<%= folder %>/<%= folder %>.module';
<% }); %>

@Module({
imports: [
//RateLimitModule, // Module de Rate Limiting
//MailerCustomModule,
//AuthModule,
ConfigModule.forRoot({
isGlobal: true,// Permet d'utiliser ConfigService dans tout le projet sans avoir à le réimporter
envFilePath: ['.env.development'],//Charge les variables d'environnement depuis le fichier .env ['.env', '.env.development', '.env.production'],
}),
TypeOrmModule.forRootAsync({
// Maon APP
imports: [ConfigModule],
inject: [ConfigService],
useFactory: getTypeOrmConfig,

}),
<% _ imports.forEach(module=>{ %>
    <%= module _%>
<% }); _%>
],
})
export class AppModule {}
