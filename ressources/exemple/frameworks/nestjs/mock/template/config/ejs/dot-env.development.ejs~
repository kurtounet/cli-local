<% if (environment.mode === 'development'){ %>
APP_PORT=<%= environment.variables.appPort %>
CORS_ORIGIN=<%= environment.variables.corsOrigine %>                        #  http://localhost:4200  Remplace par le domaine de ton frontend sinécessaire

DB_TYPE=<%= environment.variables.databaseConfig.dbType %>                  # Remplace par POSTGRES si besoin
DB_HOST=<%= environment.variables.databaseConfig.dbHost %>                  # localhost 
DB_PORT=<%= environment.variables.databaseConfig.dbPort %>                  # 3306 Msql ou  5432 pourPostgreSQL
DB_USERNAME=<%= environment.variables.databaseConfig.dbUserName %>          # Ton utilisateur MySQL/PostgreSQL
DB_PASSWORD=<% environment.variables.databaseConfig.dbPassword %>           # Ton mot de passe
DB_NAME=<%= environment.variables.databaseConfig.dbName %>                  # Nom de la base de données MySQL/PostgreSQL
DB_ENTITIES=<%= environment.variables.databaseConfig.dbEnities %>           # ["dist/**/*.entity{.ts,.js}"]
DB_SYNCHRONIZE=<%= environment.variables.databaseConfig.dbSynchronize %>    # true ⚠️ Active la synchro automatique des entités (à désactiver en prod)
    
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=ton-email@gmail.com
MAIL_PASS=ton-mot-de-passe

JWT_SECRET=super_secret_key
JWT_EXPIRES_IN=3600
JWT_REFRESH_SECRET=another_secret_key
JWT_REFRESH_EXPIRES_IN=86400  # 24 heures pour le Refresh Token
    <% } %>
