<?php

namespace App\Controller;

use App\Entity\<%= entity.namePascalCase %>;
use App\Repository\<%= entity.namePascalCase %>Repository;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Annotation\Route;

#[Route('/<%= entity.nameKebabCase %>')]
class <%= entity.namePascalCase %>Controller extends AbstractController
{
    #[Route('/', name: 'app_<%= entity.nameSnakeCase %>_index', methods: ['GET'])]
    public function index(<%= entity.namePascalCase %>Repository $<%= entity.nameCamelCase %>Repository): Response
    {
        return $this->render('<%= entity.nameSnakeCase %>/index.html.twig', [
            '<%= entity.namePluralCamelCase %>' => $<%= entity.nameCamelCase %>Repository->findAll(),
        ]);
    }
}
