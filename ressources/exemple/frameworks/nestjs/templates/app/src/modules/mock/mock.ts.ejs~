export const <%= entity.namePascalCase %>Mock = [
<% 
  for( let i=1; i<10; i++) {    
%>
{
  <% entity.columns.forEach(prop => { %>
    <% if (prop.name === 'id') { %>
      <%= prop.name %>: <%= i _%>,
    <% } else if (prop.typeSql ==='varchar' || prop.typeSql ==='text' || prop.typeSql ==='char' || prop.typeSql ==='longtext'){ %> 
      <%= prop.name %>: "<%= prop.name.toLowerCase()%>_<%=i%>",
    <% } else if (prop.typeSql === 'int') { %> 
      <%= prop.name %>: 0,
    <% } else if (prop.typeSql === 'datetime' || prop.typeSql === 'timestamp') { %> 
      <% if (prop.name != 'created_at' && prop.name != 'updated_at') { %>
        <%= prop.name %>: new Date(),
      <% } %>
    <% } else if (prop.typeSql === 'boolean') { %> 
      <%= prop.name %>: false,     
    <% } else if (prop.typeSql === 'array') { %> 
      <%= prop.name %>: [],
    <% } else if (prop.typeSql === 'object') { %> 
      <%= prop.name %>: {},   
    <% } else { %>
      <%= prop.name %>: null, 
    <% } %>     
  <% }); %>
    },
  <% } %>
];
