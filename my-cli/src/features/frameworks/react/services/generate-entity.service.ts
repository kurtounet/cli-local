import { IEntityJson } from "@parsersMdj/models/entity-json.model";

export function generateReactEntity(entity: IEntityJson) {
  console.log(`Génération de l'entitéreact pour: ${entity.namePascalCase}`);
}
