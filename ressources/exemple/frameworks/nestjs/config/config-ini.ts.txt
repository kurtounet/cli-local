import {getConfigFile} from "@frameworks/_global/utils";
import {IFramework} from "@interfaces/projet.interface";
import path from "path";


const BASE_TEMPLATES = path.resolve(__dirname, '../../nestjs/config/');
export const CONFIG_INIT_NESTJS: IFramework = {
    type: "backend",
    name: "nestjs",
    version: "11.0.0",
    cliCmd: "nest",
    gitBranch: [
        "main",
        "dev",
        "release",
        "hotfix"
    ],
    gitBranchCheckout: "dev",
    initialCommit: "string",
    installOptions: getConfigFile(path.join(BASE_TEMPLATES, "installOptions.json")),
    architecture: getConfigFile(path.join(BASE_TEMPLATES, "architecture-app.mock.json")),
    dependencies: getConfigFile(path.join(BASE_TEMPLATES, "dependencies.json")),
    // devDependencies: getConfigFile(path.join(BASE_TEMPLATES, "dependencies.json")),
    environments: getConfigFile(path.join(BASE_TEMPLATES, "environments.json")),
    script: getConfigFile(path.join(BASE_TEMPLATES, "scripts.json")),
    database: getConfigFile(path.join(BASE_TEMPLATES, "database.json")),

}