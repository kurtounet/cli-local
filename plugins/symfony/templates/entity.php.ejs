<?php

namespace App\Entity;

/**
 * Entity <%= entity.name %>
 */
class <%= entity.name %> 
{
    private $id;

<% entity.fields.forEach(field => { %>
    private $<%= field.name %>; // type: <%= field.type %>
<% }) %>

    public function getId(): ?int
    {
        return $this.id;
    }
}