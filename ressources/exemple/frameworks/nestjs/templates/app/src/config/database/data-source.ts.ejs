import "reflect-metadata";
import { DataSource } from "typeorm";

export const AppDataSource = new DataSource({
type: "mysql",  // ou "postgres" | "sqlite"
host: "localhost",
port: 3306, // 5432 pour PostgreSQL
username: "root",
password: "",
database: "my_database",
synchronize: true,  // Créé automatiquement les tables
logging: true,
entities: ["src/entities/*.ts"],  // Assurez-vous que le chemin correspond
});
