 
export default class <%= id %>Plugin {
  constructor(ctx) {
    this.ctx = ctx;
  } 
  async execute(args, data) {
    this.ctx.log.info("🚀 Démarrage du plugin <%= id %>...");     
    this.ctx.log.success("✅ Tous les blueprints ont été générés.");
  }
}
